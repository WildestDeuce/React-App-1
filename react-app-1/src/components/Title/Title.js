import React from "react";
import ReactDOM from "react-dom";
import "./Title.css";

function Title(props) {
    return <div>
        <h1>Clicky Game!</h1>
        <h2> Click pictures to increase score!!</h2>
        <h3>Clicking the same picture twice resets to zero!!</h3>
    </div>
}


export default Title;
